# Deprecations

Append-only log of deprecated agentify fields and conventions. Sorted newest-first within each release group. The `/agt-self-improve` audit reads this file as input when checking whether existing prose still references deprecated patterns.

> **Format.** Per-row table: `Field | Deprecated in | Removed in | Migration`. Each entry must point to the migration doc that introduced the deprecation. Do not edit historical rows; add new rows on top.

<!-- No deprecations yet. Future entries append above this comment, in their own version section. -->
